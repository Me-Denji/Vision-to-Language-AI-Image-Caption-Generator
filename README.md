# Vision-to-Language-AI-Image-Caption-Generator
This project builds an image captioning system using CNN and LSTM. The CNN extracts visual features from images, while the LSTM generates meaningful sentences. Trained on labeled datasets, the model learns to describe images automatically, combining computer vision and NLP techniques.
